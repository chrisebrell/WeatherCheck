//SearchBar Component - used for looking up different locaions and getting the weatherforecast for those locations

//DEPENDENCIES
import './header-styles.css'

//For temporary use, the search bar is a button, but it will be changed to a functional search bar later
function SearchBar() {
    return (
        <div>
            <button className="search-bar header">SearchBar</button>
        </div>
    )
}




export default SearchBar